# 복습

## Linear Regression
> 종속 변수가 연속적인 숫자의 값

## Logistic Regression
> 0~1 사이의 확률값 1개

## Multinomial Classification
> 클래스 별 확률값

여기까지는 정형 데이터를 대상으로 했습니다.

이제는 비정형 데이터 즉, 이미지나 소리와 같이 사람이 인지하는 데이터를 학습 시키기로 합니다.

## ANN(Artificial Neural Network)
> Neural Network, 신경망이라고도 한다

<code>Artificial Neural Network</code>는 뉴럴 네트워크 전체를 의미합니다. 그 안에 <code>DNN(Deep Neural Network)</code>이 있는데, 이것이 이름이 바뀌어 지금의 딥러닝(<code>Deep Learning</code>)입니다.

## DNN(Deap Neural Network = Deap Learning = 딥러닝)

## CNN